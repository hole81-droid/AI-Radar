# OS
.DS_Store

.claude/settings.local.json

# Obsidian 로컬 설정 (기기별 상태 — 버전 관리 제외)
.obsidian/

# 실수로 생성된 중첩 vault (루트를 vault로 다시 열면 삭제 예정)
AI radar/

# 첨부 발송용 산출물 묶음 (로컬 전용)
_outbox/

# 스캔 중 생성되는 fetch 스크래치 캐시 (위키 콘텐츠 아님, 로컬 전용)
.scratch/
tmp_scan/
raw/*/tmp/
# 2026-09-20: 스캔이 남긴 .tmp_reddit.xml이 git add -A에 딸려 들어가 무인 커밋이 막혔다.
# 디렉토리 패턴만으로는 못 잡으니 파일 접두사도 무시한다.
.tmp_*
**/.tmp_*
